import { JobProgressBarDirective } from './job-progress-bar.directive';

describe('JobProgressBarDirective', () => {
  it('should create an instance', () => {
    const directive = new JobProgressBarDirective();
    expect(directive).toBeTruthy();
  });
});
